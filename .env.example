# DocMindAI 환경 변수 예시
# 이 파일을 .env 로 복사 후 실제 값을 입력하세요:
#   cp .env.example .env

# ---------------------------------------------------------------------------
# 번역 API 키 (사용하는 엔진에 맞게 설정)
# ---------------------------------------------------------------------------
OPENAI_API_KEY=sk-...
DEEPL_API_KEY=...
GEMINI_API_KEY=...

# ---------------------------------------------------------------------------
# REST API 서버 설정
# ---------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000
# API_RELOAD=false        # true로 설정 시 파일 변경 감지 자동 재시작 (개발용)
# API_WORKERS=1           # 프로세스 수 (멀티프로세싱은 모델 공유 불가로 1 권장)

# ---------------------------------------------------------------------------
# Streamlit UI 포트
# ---------------------------------------------------------------------------
UI_PORT=8501
